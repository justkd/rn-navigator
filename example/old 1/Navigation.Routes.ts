// import { declaredRoutes } from './Routes'

// type RouteKey = (typeof declaredRoutes)[number]
// export type RoutePath = keyof typeof $routes
// export type Route = undefined | null | RoutePath

// const $routes = {
//   ...Object.fromEntries(declaredRoutes.map((k) => [k, k])),
// } as { [K in RouteKey]: RouteKey }
